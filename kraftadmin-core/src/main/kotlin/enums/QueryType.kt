package com.kraftadmin.enums

enum class QueryType {
    STORED_PROCEDURE,
    BATCH,
    SELECT,
    INSERT,
    UPDATE,
    DELETE,
    UPSERT,
    COUNT,
    EXISTS,
    AGGREGATE,
    JOIN,
    NATIVE,
    CUSTOM
}